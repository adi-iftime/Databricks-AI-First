---
description: Security audit of code or a diff using the security engineer agent
---

Load and execute as **`.cursor/agents/security-engineer.md`**. Apply the agent’s **Security Audit Report** and **`SECURITY GATE RESULT:`** `CLEAR` / `BLOCKED` workflow to the provided scope (paths, diff, or pasted code).

Return the agent’s output intact — do not drop required sections or severities.
